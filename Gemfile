source 'rubygems.org'

# Task runner
gem 'rake'

# API
gem 'sinatra', '~> 1.4', :require => 'sinatra/base'

# For rack testing helpers - better integration with rspec and rack
gem 'lookout-rack-test'

group :test do
  gem 'rspec', '~> 2.14'
  gem 'ruby-debug'
end

group :deployment do
  # Generate files to deploy on JVM
end
